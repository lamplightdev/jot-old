<div class='partial-list-order' style='text-align: right;'>
  {{#if ordering}}
    {{#each ordering.orders}}
      <a
        class='{{#ifEqual type ../currentOrdering}}current{{/ifEqual}}'
        href='#'
        data-type='{{type}}'
        data-direction='{{direction}}'
        ><svg class="icon"><use xlink:href="#{{svgname}}" /></svg></a>
    {{/each}}
  {{/if}}
</div>
